#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

struct T{
    double x;
    double y;
    double r;
}

bool comp(T a, T b){
    return a.r > b.r;
}

int main() {
    int n;
    double temp_x, temp_y;
    T temp;
    vector<T> A;
    cin >> n;
    while(n--){
        cin >> temp.x >> temp.y;
        
        
  
  return 0;
}
